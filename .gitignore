# 编译产物
**/cmd/cmd
*.exe
bin/
dist/

# Go 测试与工具产物
*.test
*.out

# 运行时产物
.run/
*.pid
*.log

# 部署用的源码压缩包（本机打包 → 上传服务器用，属于构建产物）
# 它每次打包内容都会变，进仓库只会制造无意义的 diff。
# 另外它虽然**排除了** deploy/.env 和 configs/config.prod.yaml（打包时验证过），
# 但那种"安全性靠一条 tar 命令的参数保证"的东西，最好的处理是根本不进版本库。
myfeed-src.tar.gz
*.tar.gz

# Python 缓存（scripts/ 下的压测脚本会生成）
__pycache__/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# 本地参考用的示例项目（不随仓库上传的重复副本）
#
# 这两个目录都是**别人写的**源码/文档 —— 留着本地对照用，但不该出现在本仓库的历史里。
# 之前只有 howto_feed-rebuild/ 那条，origin-feed-project_example/ 漏了，
# 结果上游项目的 141 个文件（含其设计文档与表结构图）被跟踪进了版本库。
howto_feed-rebuild/feedsystem_video_go/
origin-feed-project_example/
